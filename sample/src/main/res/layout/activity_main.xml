<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">
    <me.tom.icontablayout.IconTabLayoutViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/tabLayout"/>
    <me.tom.icontablayout.IconTabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/icon_tab_layout_height"
        android:layout_alignParentBottom="true"
        app:tabTextColor="#737373"
        app:tabSelectedTextColor="#4188F2"
        app:tabBackground="@drawable/icon_tab_layout_background"/>
</RelativeLayout>